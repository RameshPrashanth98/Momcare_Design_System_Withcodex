---
phase: 01-source-audit-and-token-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - my-design-system/package.json
  - my-design-system/tsconfig.json
  - my-design-system/src/tokens/types.ts
  - my-design-system/src/tokens/metadata.ts
  - my-design-system/src/tokens/index.ts
  - my-design-system/scripts/check-token-hardcodes.mjs
  - my-design-system/README.md
autonomous: true
requirements:
  - FND-03
  - FND-04
user_setup: []
must_haves:
  truths:
    - "The repo exposes a typed token architecture with primitive, semantic, and component alias layers."
    - "The repo has automated token validation commands that can run before broader UI phases exist."
    - "Token metadata can mark provisional source-derived items for downstream documentation."
  artifacts:
    - path: "my-design-system/package.json"
      provides: "Workspace scripts for token validation and type checking"
      contains: "\"tokens:check\""
    - path: "my-design-system/tsconfig.json"
      provides: "TypeScript configuration for token modules"
      contains: "\"include\""
    - path: "my-design-system/src/tokens/types.ts"
      provides: "Token layer and token object type definitions"
      min_lines: 25
    - path: "my-design-system/src/tokens/metadata.ts"
      provides: "Metadata helpers for source references and provisional markers"
      min_lines: 20
    - path: "my-design-system/src/tokens/index.ts"
      provides: "Public exports for token layers"
      exports: ["tokenLayers"]
    - path: "my-design-system/scripts/check-token-hardcodes.mjs"
      provides: "Automated validation for token workspace conventions"
      min_lines: 20
  key_links:
    - from: "my-design-system/package.json"
      to: "my-design-system/scripts/check-token-hardcodes.mjs"
      via: "npm script"
      pattern: "tokens:check"
    - from: "my-design-system/src/tokens/index.ts"
      to: "my-design-system/src/tokens/types.ts"
      via: "type exports"
      pattern: "from './types'"
    - from: "my-design-system/src/tokens/index.ts"
      to: "my-design-system/src/tokens/metadata.ts"
      via: "metadata exports"
      pattern: "from './metadata'"
---

<objective>
Establish the token workspace, schema, metadata conventions, and validation guardrails for Phase 1.

Purpose: Phase 1 needs a real implementation contract before value extraction starts, otherwise token data will be scattered and unverifiable.
Output: A typed token workspace with scripts, shared types, metadata helpers, and a validation script.
</objective>

<execution_context>
@D:/Codex projects/3momcare/.codex/get-shit-done/workflows/execute-plan.md
@D:/Codex projects/3momcare/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-source-audit-and-token-architecture/01-CONTEXT.md
@.planning/phases/01-source-audit-and-token-architecture/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the token workspace baseline</name>
  <files>my-design-system/package.json, my-design-system/tsconfig.json, my-design-system/README.md</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/phases/01-source-audit-and-token-architecture/01-CONTEXT.md, .planning/phases/01-source-audit-and-token-architecture/01-RESEARCH.md</read_first>
  <action>Create `my-design-system/package.json` for a Node + TypeScript workspace with scripts `typecheck` set to `tsc --noEmit` and `tokens:check` set to `node ./scripts/check-token-hardcodes.mjs`. Add minimal devDependencies for TypeScript and `@types/node`. Create `my-design-system/tsconfig.json` with `target` set to `ES2022`, `module` set to `NodeNext`, `moduleResolution` set to `NodeNext`, `declaration` false, `strict` true, `rootDir` set to `.`, and `include` covering `src/**/*.ts` and `scripts/**/*.mjs`. Update `my-design-system/README.md` so it contains a short section describing the three token layers and the two validation commands.</action>
  <verify>rg -n "\"typecheck\"|\"tokens:check\"|\"NodeNext\"|three token layers" my-design-system/package.json my-design-system/tsconfig.json my-design-system/README.md</verify>
  <acceptance_criteria>
    - `my-design-system/package.json` contains `"typecheck": "tsc --noEmit"`
    - `my-design-system/package.json` contains `"tokens:check": "node ./scripts/check-token-hardcodes.mjs"`
    - `my-design-system/tsconfig.json` contains `"module": "NodeNext"`
    - `my-design-system/tsconfig.json` contains `"strict": true`
    - `my-design-system/README.md` contains `three token layers`
  </acceptance_criteria>
  <done>Workspace scripts and TypeScript configuration exist and are documented.</done>
</task>

<task type="auto">
  <name>Task 2: Define shared token schema and metadata contracts</name>
  <files>my-design-system/src/tokens/types.ts, my-design-system/src/tokens/metadata.ts, my-design-system/src/tokens/index.ts</files>
  <read_first>.planning/phases/01-source-audit-and-token-architecture/01-CONTEXT.md, .planning/phases/01-source-audit-and-token-architecture/01-RESEARCH.md, my-design-system/package.json, my-design-system/tsconfig.json</read_first>
  <action>Create `my-design-system/src/tokens/types.ts` with explicit types for `PrimitiveToken`, `SemanticToken`, `ComponentAliasToken`, `TypographyStyleToken`, and a shared `TokenSourceRef` that captures `sourceToken`, `sourceSection`, and `provisional`. Create `my-design-system/src/tokens/metadata.ts` with metadata helpers or constants that standardize source-section names and provisional-tag handling. Create `my-design-system/src/tokens/index.ts` exporting the token-layer types, metadata helpers, and a `tokenLayers` constant with keys `primitive`, `semantic`, and `componentAlias`.</action>
  <verify>rg -n "PrimitiveToken|SemanticToken|ComponentAliasToken|TypographyStyleToken|sourceSection|provisional|tokenLayers" my-design-system/src/tokens/types.ts my-design-system/src/tokens/metadata.ts my-design-system/src/tokens/index.ts</verify>
  <acceptance_criteria>
    - `my-design-system/src/tokens/types.ts` contains `export type PrimitiveToken`
    - `my-design-system/src/tokens/types.ts` contains `export type TypographyStyleToken`
    - `my-design-system/src/tokens/types.ts` contains `provisional`
    - `my-design-system/src/tokens/index.ts` contains `tokenLayers`
    - `my-design-system/src/tokens/index.ts` contains `componentAlias`
  </acceptance_criteria>
  <done>Token layer types and metadata conventions are exported for later extraction work.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated token guardrails</name>
  <files>my-design-system/scripts/check-token-hardcodes.mjs, my-design-system/package.json</files>
  <read_first>my-design-system/package.json, my-design-system/src/tokens/types.ts, .planning/phases/01-source-audit-and-token-architecture/01-VALIDATION.md</read_first>
  <action>Create `my-design-system/scripts/check-token-hardcodes.mjs` as a Node script that scans `src/tokens/` for required exported files, verifies the presence of `provisional` metadata markers, and fails when token source files contain placeholder strings like `TODO_TOKEN`, `REPLACE_ME`, or `hardcoded later`. Keep the script deterministic and make it exit non-zero on validation failure. If needed, update `my-design-system/package.json` so the `tokens:check` script points at this file exactly.</action>
  <verify>rg -n "TODO_TOKEN|REPLACE_ME|hardcoded later|process.exit\(1\)|src/tokens" my-design-system/scripts/check-token-hardcodes.mjs && npm run tokens:check</verify>
  <acceptance_criteria>
    - `my-design-system/scripts/check-token-hardcodes.mjs` contains `TODO_TOKEN`
    - `my-design-system/scripts/check-token-hardcodes.mjs` contains `process.exit(1)`
    - `my-design-system/scripts/check-token-hardcodes.mjs` contains `src/tokens`
    - `npm run tokens:check` exits 0
  </acceptance_criteria>
  <done>Token validation can run automatically before token extraction expands.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd my-design-system && npm run typecheck`
- [ ] `cd my-design-system && npm run tokens:check`
- [ ] `my-design-system/src/tokens/index.ts` exports the layer contract and metadata helpers
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 1 has a typed token workspace and validation baseline ready for audited extraction
</success_criteria>

<output>
After completion, create `.planning/phases/01-source-audit-and-token-architecture/01-01-SUMMARY.md`
</output>
