---
phase: 02-tailwind-theme-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - my-design-system/src/tokens/tailwind-plugin.ts
  - my-design-system/tailwind.config.ts
  - my-design-system/src/tokens/index.ts
  - my-design-system/README.md
  - my-design-system/scripts/check-tailwind-contract.mjs
autonomous: true
requirements:
  - THM-01
  - THM-02
  - THM-03
user_setup: []
must_haves:
  truths:
    - "The Tailwind contract includes named editorial typography utilities derived from Phase 1 typography tokens."
    - "The Tailwind contract includes named layout helpers for container, grid, gutters, and page margins that preserve source breakpoint behavior."
    - "The Tailwind contract includes shared alias/state helpers for common patterns like button, input, card, badge, and focus ring."
  artifacts:
    - path: "my-design-system/src/tokens/tailwind-plugin.ts"
      provides: "Named utilities for typography, layout, and shared alias/state patterns"
      min_lines: 80
    - path: "my-design-system/tailwind.config.ts"
      provides: "Tailwind config wired to the shared plugin"
      contains: "plugins"
    - path: "my-design-system/src/tokens/index.ts"
      provides: "Public exports for the plugin contract"
      exports: ["morphTailwindPlugin"]
    - path: "my-design-system/README.md"
      provides: "Usage notes for the new utility categories"
      contains: "displayHero"
  key_links:
    - from: "my-design-system/tailwind.config.ts"
      to: "my-design-system/src/tokens/tailwind-plugin.ts"
      via: "plugin import"
      pattern: "morphTailwindPlugin"
    - from: "my-design-system/src/tokens/tailwind-plugin.ts"
      to: "my-design-system/src/tokens/typography.ts"
      via: "named text-style utilities"
      pattern: "displayHero"
    - from: "my-design-system/src/tokens/tailwind-plugin.ts"
      to: "my-design-system/src/tokens/component-aliases.ts"
      via: "alias utilities"
      pattern: "focusRing"
---

<objective>
Add the named utilities that complete the Phase 2 Tailwind consumption contract.

Purpose: A theme bridge alone does not satisfy the phase goal because developers also need reusable editorial, layout, and state helpers that preserve the Phase 1 decisions.
Output: Tailwind plugin utilities for typography, layout, and alias/state patterns, wired into config and validated.
</objective>

<execution_context>
@D:/Codex projects/3momcare/.codex/get-shit-done/workflows/execute-plan.md
@D:/Codex projects/3momcare/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tailwind-theme-foundation/02-CONTEXT.md
@.planning/phases/02-tailwind-theme-foundation/02-RESEARCH.md
@.planning/phases/02-tailwind-theme-foundation/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add editorial typography and layout utilities</name>
  <files>my-design-system/src/tokens/tailwind-plugin.ts, my-design-system/tailwind.config.ts</files>
  <read_first>my-design-system/src/tokens/tailwind-theme.ts, my-design-system/src/tokens/typography.ts, my-design-system/src/tokens/primitives.ts, my-design-system/tailwind.config.ts, .planning/phases/02-tailwind-theme-foundation/02-CONTEXT.md</read_first>
  <action>Create `my-design-system/src/tokens/tailwind-plugin.ts` exporting `morphTailwindPlugin`. Register named typography utilities for `displayHero`, `displaySection`, `bodyLg`, `bodyMd`, `labelSm`, `eyebrow`, and `caption`, using the existing token values and responsive overrides from `typographyTokens`. In the same plugin, register layout helpers that cover a container utility, a grid utility, desktop/tablet/mobile column helpers, breakpoint-aware gutter helpers, and breakpoint-aware page margin helpers built from `gridPrimitives`. Update `my-design-system/tailwind.config.ts` so the config includes this plugin in its `plugins` array.</action>
  <verify>rg -n "morphTailwindPlugin|displayHero|displaySection|bodyLg|bodyMd|labelSm|eyebrow|caption|gridColsDesktop|gridGutterLg|gridMarginLg|plugins" my-design-system/src/tokens/tailwind-plugin.ts my-design-system/tailwind.config.ts</verify>
  <acceptance_criteria>
    - `my-design-system/src/tokens/tailwind-plugin.ts` contains `export const morphTailwindPlugin`
    - `my-design-system/src/tokens/tailwind-plugin.ts` contains `displayHero`
    - `my-design-system/src/tokens/tailwind-plugin.ts` contains `gridGutterLg`
    - `my-design-system/src/tokens/tailwind-plugin.ts` contains `gridMarginLg`
    - `my-design-system/tailwind.config.ts` contains `plugins`
  </acceptance_criteria>
  <done>Named editorial typography and layout utilities are registered in the Tailwind contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add shared alias and state utilities</name>
  <files>my-design-system/src/tokens/tailwind-plugin.ts, my-design-system/src/tokens/index.ts</files>
  <read_first>my-design-system/src/tokens/component-aliases.ts, my-design-system/src/tokens/semantic.ts, my-design-system/src/tokens/tailwind-plugin.ts, my-design-system/src/tokens/index.ts, .planning/phases/02-tailwind-theme-foundation/02-CONTEXT.md</read_first>
  <action>Extend `my-design-system/src/tokens/tailwind-plugin.ts` so `morphTailwindPlugin` also registers shared alias utilities driven by `componentAliases` for button primary background/text/hover, input border/background/focus, card background/border/shadow/radius, badge background/text/radius, and focus ring color/width/offset. Keep these utilities shared-pattern oriented and do not invent variant systems beyond the current alias set. Update `my-design-system/src/tokens/index.ts` to export `morphTailwindPlugin` from `./tailwind-plugin.js` without removing previous exports.</action>
  <verify>rg -n "button|primaryHover|input|borderFocus|card|badge|focusRing|morphTailwindPlugin" my-design-system/src/tokens/tailwind-plugin.ts my-design-system/src/tokens/index.ts</verify>
  <acceptance_criteria>
    - `my-design-system/src/tokens/tailwind-plugin.ts` contains `primaryHover`
    - `my-design-system/src/tokens/tailwind-plugin.ts` contains `borderFocus`
    - `my-design-system/src/tokens/tailwind-plugin.ts` contains `focusRing`
    - `my-design-system/src/tokens/index.ts` contains `morphTailwindPlugin`
    - `my-design-system/src/tokens/tailwind-plugin.ts` contains `badge`
  </acceptance_criteria>
  <done>The Tailwind contract exposes shared state and alias helpers derived from the token source of truth.</done>
</task>

<task type="auto">
  <name>Task 3: Document the utility contract and tighten validation</name>
  <files>my-design-system/README.md, my-design-system/scripts/check-tailwind-contract.mjs</files>
  <read_first>my-design-system/README.md, my-design-system/scripts/check-tailwind-contract.mjs, my-design-system/src/tokens/tailwind-plugin.ts, my-design-system/tailwind.config.ts, .planning/phases/02-tailwind-theme-foundation/02-VALIDATION.md</read_first>
  <action>Update `my-design-system/README.md` with a short Tailwind usage section that lists the editorial utility names `displayHero`, `displaySection`, `bodyLg`, `bodyMd`, `labelSm`, `eyebrow`, and `caption`; describes the layout helpers for container, grid, gutters, and margins; and states that alias utilities cover button, input, card, badge, and focus ring patterns. Extend `my-design-system/scripts/check-tailwind-contract.mjs` so it also verifies the plugin file exists, checks for the typography utility names and alias keys above, and fails if placeholder strings such as `TODO_PLUGIN`, `REPLACE_PLUGIN`, or `add classes later` appear in the plugin contract.</action>
  <verify>rg -n "displayHero|displaySection|bodyLg|bodyMd|labelSm|eyebrow|caption|button|input|card|badge|focus ring|TODO_PLUGIN|REPLACE_PLUGIN|add classes later" my-design-system/README.md my-design-system/scripts/check-tailwind-contract.mjs && npm run tailwind:check</verify>
  <acceptance_criteria>
    - `my-design-system/README.md` contains `displayHero`
    - `my-design-system/README.md` contains `focus ring`
    - `my-design-system/scripts/check-tailwind-contract.mjs` contains `TODO_PLUGIN`
    - `my-design-system/scripts/check-tailwind-contract.mjs` contains `displayHero`
    - `npm run tailwind:check` exits 0
  </acceptance_criteria>
  <done>The utility contract is documented and the validator checks the final Phase 2 surface.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd my-design-system && npm run typecheck`
- [ ] `cd my-design-system && npm run tokens:check`
- [ ] `cd my-design-system && npm run tailwind:check`
- [ ] `my-design-system/src/tokens/tailwind-plugin.ts` exposes typography, layout, and alias/state helpers
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 2 exposes reusable Tailwind utilities for semantic theming, layout, typography, and shared states
</success_criteria>

<output>
After completion, create `.planning/phases/02-tailwind-theme-foundation/02-02-SUMMARY.md`
</output>
