---
phase: 02-tailwind-theme-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - my-design-system/package.json
  - my-design-system/README.md
  - my-design-system/tailwind.config.ts
  - my-design-system/src/tokens/tailwind-theme.ts
  - my-design-system/src/tokens/index.ts
  - my-design-system/scripts/check-tailwind-contract.mjs
autonomous: true
requirements:
  - THM-01
  - THM-03
user_setup: []
must_haves:
  truths:
    - "The package exports a typed Tailwind-facing theme contract derived from the Phase 1 token modules."
    - "Tailwind consumers get semantic-first token mappings while primitive mappings remain available but secondary."
    - "The repo can validate Tailwind contract integrity with an automated command before component phases begin."
  artifacts:
    - path: "my-design-system/package.json"
      provides: "Tailwind tooling dependencies and validation scripts"
      contains: "\"tailwind:check\""
    - path: "my-design-system/tailwind.config.ts"
      provides: "Package-local Tailwind entry point wired to token-derived helpers"
      contains: "morphTailwindTheme"
    - path: "my-design-system/src/tokens/tailwind-theme.ts"
      provides: "Typed theme extension derived from primitives, semantic tokens, aliases, and typography"
      min_lines: 60
    - path: "my-design-system/src/tokens/index.ts"
      provides: "Public exports for Tailwind helpers"
      exports: ["morphTailwindTheme"]
    - path: "my-design-system/scripts/check-tailwind-contract.mjs"
      provides: "Automated validation for Tailwind contract files and exports"
      min_lines: 30
  key_links:
    - from: "my-design-system/tailwind.config.ts"
      to: "my-design-system/src/tokens/tailwind-theme.ts"
      via: "theme import"
      pattern: "morphTailwindTheme"
    - from: "my-design-system/package.json"
      to: "my-design-system/scripts/check-tailwind-contract.mjs"
      via: "npm script"
      pattern: "tailwind:check"
    - from: "my-design-system/src/tokens/index.ts"
      to: "my-design-system/src/tokens/tailwind-theme.ts"
      via: "public export"
      pattern: "tailwind-theme"
---

<objective>
Establish the Tailwind contract baseline for Phase 2.

Purpose: Tailwind cannot consume the Phase 1 token system consistently until the repo exports a typed theme bridge, a package-local config entry point, and an automated contract check.
Output: Tailwind dependencies, typed theme helpers, config wiring, public exports, and contract validation.
</objective>

<execution_context>
@D:/Codex projects/3momcare/.codex/get-shit-done/workflows/execute-plan.md
@D:/Codex projects/3momcare/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tailwind-theme-foundation/02-CONTEXT.md
@.planning/phases/02-tailwind-theme-foundation/02-RESEARCH.md
@.planning/phases/02-tailwind-theme-foundation/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Tailwind workspace dependencies and validation entry points</name>
  <files>my-design-system/package.json, my-design-system/README.md</files>
  <read_first>my-design-system/package.json, my-design-system/README.md, .planning/phases/02-tailwind-theme-foundation/02-CONTEXT.md, .planning/phases/02-tailwind-theme-foundation/02-RESEARCH.md, .planning/phases/02-tailwind-theme-foundation/02-VALIDATION.md</read_first>
  <action>Update `my-design-system/package.json` to add Tailwind devDependencies `tailwindcss`, `postcss`, and `autoprefixer`. Add a script `tailwind:check` set to `node ./scripts/check-tailwind-contract.mjs`. Keep the existing `typecheck` and `tokens:check` scripts intact. Update `my-design-system/README.md` with a short Tailwind contract section that states semantic utilities are the default authoring surface, primitive mappings are secondary, and the validation commands are `npm run typecheck`, `npm run tokens:check`, and `npm run tailwind:check`.</action>
  <verify>rg -n "\"tailwindcss\"|\"postcss\"|\"autoprefixer\"|\"tailwind:check\"|semantic utilities are the default authoring surface" my-design-system/package.json my-design-system/README.md</verify>
  <acceptance_criteria>
    - `my-design-system/package.json` contains `"tailwind:check": "node ./scripts/check-tailwind-contract.mjs"`
    - `my-design-system/package.json` contains `"tailwindcss"`
    - `my-design-system/package.json` contains `"postcss"`
    - `my-design-system/package.json` contains `"autoprefixer"`
    - `my-design-system/README.md` contains `semantic utilities are the default authoring surface`
  </acceptance_criteria>
  <done>Tailwind tooling and validation entry points are declared in the workspace.</done>
</task>

<task type="auto">
  <name>Task 2: Create the typed Tailwind theme adapter</name>
  <files>my-design-system/src/tokens/tailwind-theme.ts, my-design-system/src/tokens/index.ts</files>
  <read_first>my-design-system/src/tokens/primitives.ts, my-design-system/src/tokens/semantic.ts, my-design-system/src/tokens/component-aliases.ts, my-design-system/src/tokens/typography.ts, my-design-system/src/tokens/index.ts, .planning/phases/02-tailwind-theme-foundation/02-CONTEXT.md</read_first>
  <action>Create `my-design-system/src/tokens/tailwind-theme.ts` exporting a typed `morphTailwindTheme` object built from the existing token modules. Include semantic-first `colors` groups for `surface`, `text`, `border`, `interactive`, and `feedback`; include a clearly secondary primitive color namespace such as `brand`; map `spacing`, `boxShadow`, `borderRadius`, `borderWidth`, `fontFamily`, `fontWeight`, `letterSpacing`, `lineHeight`, `fontSize`, `maxWidth`, and `screens` from the existing token objects; and expose named alias groups needed by later utilities. Update `my-design-system/src/tokens/index.ts` to export `morphTailwindTheme` from `./tailwind-theme.js` without removing existing token exports.</action>
  <verify>rg -n "morphTailwindTheme|surface|interactive|feedback|boxShadow|borderRadius|fontFamily|screens|brand" my-design-system/src/tokens/tailwind-theme.ts my-design-system/src/tokens/index.ts</verify>
  <acceptance_criteria>
    - `my-design-system/src/tokens/tailwind-theme.ts` contains `export const morphTailwindTheme`
    - `my-design-system/src/tokens/tailwind-theme.ts` contains `surface`
    - `my-design-system/src/tokens/tailwind-theme.ts` contains `interactive`
    - `my-design-system/src/tokens/tailwind-theme.ts` contains `screens`
    - `my-design-system/src/tokens/index.ts` contains `morphTailwindTheme`
  </acceptance_criteria>
  <done>The repo has a typed Tailwind theme bridge derived from the token source of truth.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Tailwind config and add contract validation</name>
  <files>my-design-system/tailwind.config.ts, my-design-system/scripts/check-tailwind-contract.mjs, my-design-system/package.json</files>
  <read_first>my-design-system/package.json, my-design-system/src/tokens/tailwind-theme.ts, my-design-system/src/tokens/index.ts, .planning/phases/02-tailwind-theme-foundation/02-VALIDATION.md</read_first>
  <action>Create `my-design-system/tailwind.config.ts` that imports `morphTailwindTheme`, exports a default Tailwind config object, and places the token mappings under `theme.extend`. Point `content` at `./src/**/*.{ts,tsx,mdx}` and `./stories/**/*.{ts,tsx,mdx}` so later phases have the standard scanning paths ready. Create `my-design-system/scripts/check-tailwind-contract.mjs` as a deterministic Node script that verifies `tailwind.config.ts`, `src/tokens/tailwind-theme.ts`, and `src/tokens/index.ts` exist; checks for `morphTailwindTheme`, `theme.extend`, and the semantic groups `surface`, `text`, `border`, and `interactive`; and exits non-zero if placeholder strings such as `TODO_TAILWIND`, `REPLACE_TAILWIND`, or `wire later` appear in the Tailwind contract files. Keep `my-design-system/package.json` pointing `tailwind:check` at this script exactly.</action>
  <verify>rg -n "morphTailwindTheme|theme:\s*\{|theme.extend|surface|text|border|interactive|TODO_TAILWIND|REPLACE_TAILWIND|wire later|process.exit\(1\)" my-design-system/tailwind.config.ts my-design-system/scripts/check-tailwind-contract.mjs && npm run tailwind:check</verify>
  <acceptance_criteria>
    - `my-design-system/tailwind.config.ts` contains `morphTailwindTheme`
    - `my-design-system/tailwind.config.ts` contains `content:`
    - `my-design-system/scripts/check-tailwind-contract.mjs` contains `TODO_TAILWIND`
    - `my-design-system/scripts/check-tailwind-contract.mjs` contains `process.exit(1)`
    - `npm run tailwind:check` exits 0
  </acceptance_criteria>
  <done>Tailwind config wiring and automated contract checks exist before utility work starts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd my-design-system && npm run typecheck`
- [ ] `cd my-design-system && npm run tokens:check`
- [ ] `cd my-design-system && npm run tailwind:check`
- [ ] `my-design-system/tailwind.config.ts` imports `morphTailwindTheme`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 2 has a typed Tailwind baseline ready for layout, typography, and alias utilities
</success_criteria>

<output>
After completion, create `.planning/phases/02-tailwind-theme-foundation/02-01-SUMMARY.md`
</output>
