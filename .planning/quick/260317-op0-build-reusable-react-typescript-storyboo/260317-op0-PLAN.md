# Quick Task 260317-op0: Build reusable React + TypeScript + Storybook components for the Clinic Session screen using Material 3 patterns and our design tokens from src/tokens/ only. No hardcoded values. - Plan

**Date:** 2026-03-17
**Status:** Complete

## Objective

Add a reusable Clinic Session component family and composed mobile screen using token-driven Material 3 patterns, while extending shared design-system components only where the requested props require it.

## Tasks

### 1. Extend shared primitives where the screen contract requires it
- Expand `SectionHeader` to support subtitle and trailing action content.
- Expand `StatusChip` to support the requested session statuses while keeping the existing API usable.
- Accept `activeItem` and `onChange` aliases on `NavigationBar` without breaking current consumers.

### 2. Build the clinic-session component family
- Create `Switch`, `MetadataRow`, `ClinicSummaryCard`, `MotherSessionCard`, `BottomActionBar`, and `ClinicSessionLayout`.
- Use only token values and existing shared primitives for layout, color, type, spacing, and interaction states.
- Keep all new controls keyboard accessible and Storybook-friendly.

### 3. Compose and document the screen
- Add `src/screens/ClinicSession/ClinicSessionScreen.tsx` and its Storybook screen story.
- Add per-component stories plus a Clinic Session primitives/composition story.
- Verify with `npm run typecheck`, `npm run tokens:check`, and `npm run tailwind:check`.
