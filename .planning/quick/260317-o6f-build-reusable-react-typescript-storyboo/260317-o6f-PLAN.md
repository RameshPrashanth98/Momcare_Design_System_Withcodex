# Quick Task 260317-o6f: Build reusable React + TypeScript + Storybook components for the Add Clinic Session screen using Material 3 patterns and our design tokens from src/tokens/ only. No hardcoded values. - Plan

**Date:** 2026-03-17
**Status:** Complete

## Objective

Ship a reusable, mobile-first Add Clinic Session form surface in `my-design-system` using token-driven styling, Storybook CSF3 stories, and accessible Material 3-inspired form patterns.

## Tasks

### 1. Extend shared primitives for clinic-form needs
- Update `Button` to support the required outlined action pattern.
- Update `TextField` to expose the requested `error` state contract without breaking existing usage.
- Expand the shared icon set with date/time/dropdown affordances needed by the new form components.

### 2. Add reusable clinic form components and layout
- Create `DateField`, `TimeField`, `SelectField`, `FormSection`, and `AddClinicSessionLayout` under `src/components/`.
- Keep all visual styling token-driven through `src/tokens/` and the existing component aliases.
- Preserve keyboard accessibility and Storybook-friendly controlled APIs.

### 3. Compose the Add Clinic Session screen and document it
- Create `src/screens/AddClinicSession/AddClinicSessionScreen.tsx` and its Storybook screen story.
- Add component stories for the new clinic form pieces.
- Verify with `npm run typecheck`, `npm run tokens:check`, and `npm run tailwind:check`.
