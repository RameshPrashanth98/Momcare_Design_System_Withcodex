# Quick Task 260317-k6j: Create reusable Storybook notifications components for the Mom Care app - Plan

**Date:** 2026-03-17
**Status:** In Progress

## Objective

Add reusable token-driven components and Storybook stories for the Notifications flow by extending shared primitives and creating the missing screen-level building blocks.

## Tasks

### 1. Extend shared primitives for notifications
- Files: `my-design-system/src/components/IconButton/*`, `my-design-system/src/components/SectionHeader/*`, `my-design-system/src/components/Button/*`
- Action: Update stories and shared button capabilities so notifications-specific use cases fit the existing component APIs.
- Verify: Shared components cover back/filter/avatar icon buttons, simple section headers, and filled/tonal button actions.
- Done: Notifications can reuse existing primitives without bespoke variants.

### 2. Add notifications-specific components
- Files: `my-design-system/src/components/SegmentedButton/*`, `my-design-system/src/components/NotificationCard/*`, `my-design-system/src/components/TopAppBar/*`, `my-design-system/src/components/NotificationsLayout/*`
- Action: Implement segmented filters, notification cards, top app bar, layout shell, and named exports.
- Verify: Components compile with typed props, accessible semantics, keyboard support, and token-only styling.
- Done: Notifications screen can be assembled from reusable components.

### 3. Validate and record the task
- Files: `.planning/STATE.md`, `.planning/quick/260317-k6j-create-reusable-storybook-notifications-/260317-k6j-SUMMARY.md`
- Action: Run repo checks and record the completed task in planning artifacts.
- Verify: `npm run typecheck` and `npm run tokens:check` pass from `my-design-system/`.
- Done: Summary and state entries reflect the notifications component task.
