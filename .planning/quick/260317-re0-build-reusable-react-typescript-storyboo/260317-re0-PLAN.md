# Quick Task 260317-re0: Build reusable React + TypeScript + Storybook components for the Vaccination Records screen using Material 3 patterns and our design tokens from src/tokens/ only. No hardcoded values. - Plan

**Date:** 2026-03-17
**Status:** Complete

## Objective

Add a reusable Vaccination Records screen family with a contextual mother summary, vaccination history and upcoming record cards, and a floating primary action while reusing shared M3 shell primitives wherever possible.

## Tasks

### 1. Align shared primitives for the screen shell
- Reuse the existing `TopAppBar`, `IconButton`, `LanguageSwitcher`, `SectionHeader`, `StatusChip`, `ExtendedFAB`, `NavigationBar`, and `Typography` primitives.
- Adjust `ContextHeader` so the back affordance is truly optional for screens that only need contextual identity text.
- Add a generic token-driven `Divider` primitive for grouped record sections.

### 2. Build the Vaccination Records component family
- Create `MotherSummaryCard`, `VaccinationRecordCard`, and `VaccinationRecordsLayout`.
- Keep the new cards mobile-first, keyboard accessible, and fully token-driven.

### 3. Compose and document the screen
- Add `src/screens/VaccinationRecords/VaccinationRecordsScreen.tsx` and the screen story.
- Add Storybook stories for the new vaccination-records components and divider primitive.
- Verify with `npm run typecheck`, `npm run tokens:check`, and `npm run tailwind:check`.
