# Quick Task 260316-ju1 Plan

**Description:** We are building the Mom Care app frontend and want to adapt the Material 3 design system from https://m3.material.io/ to our brand style guide and hi-fi wireframes. The current goal is to create reusable Storybook components needed for the authentication/login screen.

## Objective
Ship token-backed authentication primitives and Storybook stories for the login flow so Phase 3 can inherit reusable, documented components instead of one-off auth UI.

## Tasks
1. Expand `Button` for auth flow needs, including full-width layout support and auth-oriented stories.
2. Add reusable `TextField` and `PasswordField` components with Storybook coverage for login states.
3. Add a reusable `Checkbox` component for the remember-me pattern and verify the component set with `npm run typecheck`, `npm run build-storybook`, and `npm run tokens:check`.
