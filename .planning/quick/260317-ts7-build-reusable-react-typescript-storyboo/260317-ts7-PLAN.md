# Quick Task 260317-ts7: Build reusable React + TypeScript + Storybook components for the User Profile screen using Material 3 patterns and our design tokens from src/tokens/ only. No hardcoded values. - Plan

**Date:** 2026-03-17
**Status:** Complete

## Objective

Add a reusable User Profile screen family with a profile summary card, grouped settings rows, segmented language preference control, and logout action while reusing the shared app shell, switch, and button primitives already present in the design system.

## Tasks

### 1. Reuse and adapt existing shared settings primitives
- Reuse the existing `TopAppBar`, `IconButton`, `LanguageSwitcher`, `SectionHeader`, `Switch`, `Button`, `NavigationBar`, `Typography`, and `Divider` components.
- Add a small `Avatar` primitive and a `SegmentedControl` wrapper so the screen can use repo-consistent APIs without duplicating the underlying interaction logic.

### 2. Build the User Profile component family
- Create `ProfileSummaryCard`, `SettingsGroup`, `SettingsRow`, and `UserProfileLayout`.
- Keep the settings groups, rows, segmented control, and profile summary fully token-driven, mobile-first, and keyboard accessible.

### 3. Compose and document the screen
- Add `src/screens/UserProfile/UserProfileScreen.tsx` and the screen story.
- Add Storybook stories for each new user-profile component and the new small primitives.
- Verify with `npm run typecheck`, `npm run tokens:check`, and `npm run tailwind:check`.
