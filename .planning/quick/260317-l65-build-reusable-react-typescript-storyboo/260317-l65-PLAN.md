# Quick Task 260317-l65: Build reusable React + TypeScript + Storybook components for the Mother Profile screen using Material 3 patterns and our design tokens from src/tokens/ only. No hardcoded values. - Plan

**Date:** 2026-03-17
**Status:** Complete

## Objective

Build a reusable Mother Profile screen component set in `my-design-system` using React, TypeScript, Storybook CSF3, existing design tokens from `src/tokens/`, and Material 3 inspired layout patterns.

## Tasks

### 1. Add Mother Profile primitives and composition layer
- Files: `my-design-system/src/components/MotherProfile/*`, `my-design-system/src/components/ListItem/ListItem.tsx`
- Action: Implement token-driven profile-specific components and align `ListItem` with the requested prop names.
- Verify: Components are reusable, mobile-first, keyboard accessible, and rely on shared tokens.
- Done: `SummaryCard`, `InfoRow`, `MetricRow`, `MetricIndicator`, `SectionCard`, `MotherProfileLayout`, supporting icons, and `ListItem` prop aliases are in place.

### 2. Compose the full Mother Profile screen
- Files: `my-design-system/src/screens/MotherProfile/*`
- Action: Assemble the requested hi-fi screen using existing shared primitives plus the new Mother Profile components.
- Verify: Screen includes the top app bar, summary card, grouped sections, visit history, and bottom navigation.
- Done: `MotherProfileScreen.tsx` and its Storybook story are implemented.

### 3. Document and validate in Storybook
- Files: Mother Profile story files and Storybook output
- Action: Add CSF3 stories for the new components and a shared-primitives showcase, then run validation.
- Verify: `npm run typecheck`, `npm run tokens:check`, and `npm run build-storybook` all succeed.
- Done: Story coverage and validation are complete.
