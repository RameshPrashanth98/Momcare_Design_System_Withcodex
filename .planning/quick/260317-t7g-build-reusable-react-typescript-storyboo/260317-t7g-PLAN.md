# Quick Task 260317-t7g: Build reusable React + TypeScript + Storybook components for the Pregnancy Timeline screen using Material 3 patterns and our design tokens from src/tokens/ only. No hardcoded values. - Plan

**Date:** 2026-03-17
**Status:** Complete

## Objective

Add a reusable Pregnancy Timeline screen family with a custom timeline surface, milestone list, and report-category chip filtering while reusing the shared records shell and summary-card primitives already present in the design system.

## Tasks

### 1. Extend shared records-shell primitives where the screen contract differs
- Reuse the existing `TopAppBar`, `IconButton`, `LanguageSwitcher`, `ContextHeader`, `SectionHeader`, `StatusChip`, `Button`, `NavigationBar`, `Typography`, and `Divider` components.
- Extend `MotherSummaryCard` to support an optional `edd` field while preserving the earlier records-screen usage.
- Reuse the existing `FilterChip` and `FilterChipGroup` from Lab Reports.

### 2. Build the Pregnancy Timeline component family
- Create `TimelineItem`, `PregnancyTimelineCard`, `MilestoneItem`, `MilestoneListCard`, and `PregnancyTimelineLayout`.
- Keep the custom timeline pattern, milestone list, and overall layout token-driven, mobile-first, and keyboard accessible.

### 3. Compose and document the screen
- Add `src/screens/PregnancyTimeline/PregnancyTimelineScreen.tsx` and the screen story.
- Add Storybook stories for each new pregnancy-timeline component and the shared summary-card extension.
- Verify with `npm run typecheck`, `npm run tokens:check`, and `npm run tailwind:check`.
